# DreadGOAD CLI Configuration
env: prod
provider: aws  # Provider: aws, azure, proxmox, ludus
region: us-east-1  # Fallback AWS region for environments that don't set their own
instance_profile: WarpgateImageBuilderInstanceProfile  # IAM instance profile for EC2 Image Builder
debug: false
max_retries: 3
retry_delay: 30
idle_timeout: 3600
# log_dir: ~/.ansible/logs/goad
# project_root: /path/to/DreadGOAD  # Auto-detected if omitted

# Proxmox settings (used when provider: proxmox)
# proxmox:
#   api_url: "https://192.168.20.80:8006"
#   user: "root@pam"
#   password: ""  # Or set DREADGOAD_PROXMOX_PASSWORD env var
#   node: "proxmox"
#   pool: "GOAD"
#   full_clone: "false"
#   storage: "local"
#   vlan: "10"
#   network_bridge: "vmbr3"
#   network_model: "e1000"
#   ip_range: "192.168.10"
#   lab: "GOAD"
#   template_ids:
#     WinServer2019_x64: "201900"
#     WinServer2016_x64: "201600"
#     WinServer2022_x64: "202201"
#     WinServer2025_x64: "202501"
#     Ubuntu_2204_x64: "922040"
#     Ubuntu_2404_x64: "924040"

# Ludus settings (used when provider: ludus)
# ludus:
#   api_key: ""  # Or set LUDUS_API_KEY env var
#   use_impersonation: true
#   ssh_host: ""  # Remote Ludus host for SSH-based execution
#   ssh_user: "root"
#   ssh_password: ""  # Or set LUDUS_SSH_PASSWORD env var
#   ssh_key_path: ""  # Path to SSH private key
#   ssh_port: 22

# Per-environment settings
environments:
  dev:
    variant: true
    variant_source: ad/GOAD
    variant_target: ad/GOAD-variant-1
    variant_name: variant-1
    vpc_cidr: "10.0.0.0/16"
  staging:
    # The deployed staging lab is stock GOAD (kingslanding/sevenkingdoms). Setting
    # variant: true here makes provision/ad-users generate a fresh random identity
    # and silently repoint the env at it, so playbooks target a lab that does not exist.
    variant: false
    region: us-west-1
    vpc_cidr: "10.1.0.0/16"
  prod:
    region: us-east-1
    vpc_cidr: "10.2.0.0/16"
  test:
    variant: true
    variant_source: ad/GOAD
    variant_target: ad/GOAD-dreadindex
    variant_name: dreadindex
    region: us-east-2
    vpc_cidr: "10.8.0.0/16"
  dreadindex:
    variant: true
    variant_source: ad/GOAD
    variant_target: ad/GOAD-dreadindex
    variant_name: dreadindex
    vpc_cidr: "10.1.0.0/16"
