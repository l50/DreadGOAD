---
- name: Laps dc install
  ansible.builtin.import_tasks: install.yml
  when: laps_path != false

- name: Move to laps ou
  ansible.builtin.import_tasks: move_server_to_ou.yml
  when: laps_path != false
