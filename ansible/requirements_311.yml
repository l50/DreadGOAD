rich
psutil
Jinja2
pyyaml
# Ansible
# setuptools for python >= 3.12
setuptools
ansible_runner
# IMPORTANT: ansible-core >=2.19 breaks Windows module execution over AWS SSM.
# See requirements.txt for full explanation.
# ansible-core 2.17.x is the latest series compatible with the SSM plugin.
ansible-core>=2.17.0,<2.18.0
pywinrm
# AZURE
azure-identity
azure-mgmt-compute
azure-mgmt-network
# AWS
boto3
# Proxmox
proxmoxer
requests
