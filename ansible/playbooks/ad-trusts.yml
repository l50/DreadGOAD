---

# set AD trusts ==================================================================================================
- name: Trusts configuration prepare
  hosts: trust
  roles:
    - { role: 'dreadnode.goad.settings_disable_nat_adapter', tags: 'disable_nat_adapter'}
    - { role: 'dreadnode.goad.dns_conditional_forwarder', tags: 'dns_conditional_forwarder' }
  vars:
    domain: "{{ lab.hosts[dict_key].domain }}"
    remote_forest: "{{ lab.domains[domain].trust }}"
    zone_name: "{{ remote_forest }}"
    remote_dc: "{{ lab.domains[remote_forest].dc }}"
    instance_to_ip: "{{ hostvars[groups['dc'][0]]['instance_to_ip'] | default({}) }}"
    master_server: "{{ hostvars[remote_dc]['dc_ipv4'] | default(instance_to_ip[hostvars[remote_dc]['ansible_host']] | default(hostvars[remote_dc].ansible_host)) }}"
    replication: forest
    dc_hostname_to_ip: "{{ hostvars[groups['dc'][0]]['dc_hostname_to_ip'] | default({}) }}"

- name: Trusts configuration
  hosts: trust
  serial: 1 # add one trust at a time to avoid issues
  roles:
  - { role: 'dreadnode.goad.trusts', tags: 'trust' }
  vars:
    domain: "{{ lab.hosts[dict_key].domain }}"
    domain_username: "{{ domain }}\\{{ admin_user }}"
    domain_password: "{{ lab.domains[domain].domain_password }}"
    remote_forest: "{{ lab.domains[domain].trust }}"
    remote_admin: "{{ admin_user }}@{{ remote_forest }}"
    remote_admin_password: "{{ lab.domains[remote_forest].domain_password }}"
    zone_name: "{{ remote_forest }}"
    remote_dc: "{{ lab.domains[remote_forest].dc }}"
    instance_to_ip: "{{ hostvars[groups['dc'][0]]['instance_to_ip'] | default({}) }}"
    master_server: "{{ hostvars[remote_dc]['dc_ipv4'] | default(instance_to_ip[hostvars[remote_dc]['ansible_host']] | default(hostvars[remote_dc].ansible_host)) }}"
    dc_hostname_to_ip: "{{ hostvars[groups['dc'][0]]['dc_hostname_to_ip'] | default({}) }}"

- name: Trusts configuration end
  hosts: trust
  roles:
  - { role: 'dreadnode.goad.settings_enable_nat_adapter', tags: 'enable_nat_adapter'}

- name: Adjust DNS conditional forwarded configuration
  hosts: trust
  roles:
  - { role: 'dreadnode.goad.dc_dns_conditional_forwarder', tags: 'dns_conditional_forwarder' }
  vars:
    domain: "{{ lab.hosts[dict_key].domain }}"
    domain_username: "{{ domain }}\\{{ admin_user }}"
    domain_password: "{{ lab.domains[domain].domain_password }}"
    parent_domain: "{{ '.'.join(domain.split('.')[1:]) | default('') }}"
    trust: "{{ lab.domains[domain].trust | default('') }}"
    domains: "{{ lab.domains.keys() }}"
    replication: forest
    dc_hostname_to_ip: "{{ hostvars[groups['dc'][0]]['dc_hostname_to_ip'] | default({}) }}"
    instance_to_ip: "{{ hostvars[groups['dc'][0]]['instance_to_ip'] | default({}) }}"
